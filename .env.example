# Opción A — Redis por TCP (Redis Cloud, etc.)
REDIS_URL=redis://default:PASSWORD@host:port

# Opción B — REST (Upstash / integración Vercel que expone KV_REST_*)
KV_REST_API_URL=
KV_REST_API_TOKEN=

# Si definís REDIS_URL, se usa esa y se ignoran KV_REST_*.
